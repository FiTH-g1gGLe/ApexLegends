"VideoConfig"
{
    "setting.cl_gib_allow"                          "0"               // Disallow gibs (exploding body parts)
    "setting.cl_particle_fallback_base"             "-1"              // Particle fallback base level
    "setting.cl_particle_fallback_multiplier"       "-1"              // Particle fallback multiplier
    "setting.cl_ragdoll_maxcount"                   "0"               // Maximum number of ragdolls allowed
    "setting.cl_ragdoll_self_collision"             "0"               // Disable ragdoll self-collision
    "setting.mat_forceaniso"                        "0"               // Disable forced anisotropic filtering
    "setting.mat_mip_linear"                        "0"               // Disable linear mipmap filtering
    "setting.stream_memory"                         "0"               // Disable streaming memory
    "setting.mat_picmip"                            "4"               // Set texture mipmap level (higher means lower quality)
    "setting.particle_cpu_level"                    "0"               // CPU level for particle effects
    "setting.r_createmodeldecals"                   "0"               // Disable creation of model decals
    "setting.r_decals"                              "0"               // Disable decals
    "setting.r_lod_switch_scale"                    "0.3"             // LOD switch scale factor
    "setting.shadow_enable"                         "0"               // Disable shadows
    "setting.shadow_depth_dimen_min"                "0"               // Minimum shadow depth dimension
    "setting.shadow_depth_upres_factor_max"         "0"               // Max upres factor for shadow depth
    "setting.shadow_maxdynamic"                     "0"               // Max number of dynamic shadows
    "setting.ssao_enabled"                          "0"               // Disable SSAO (Screen Space Ambient Occlusion)
    "setting.ssao_downsample"                       "0"               // SSAO downsample level
    "setting.dvs_enable"                            "0"               // Disable DVS (Dynamic Visual Settings)
    "setting.dvs_gpuframetime_min"                  "9500"            // Minimum GPU frame time for DVS
    "setting.dvs_gpuframetime_max"                  "9800"            // Maximum GPU frame time for DVS
    "setting.sound_volume"                          "1"               // Set sound volume (1 is maximum)
    "setting.last_display_width"                    "3840"            // Last display width
    "setting.last_display_height"                   "2160"            // Last display height
    "setting.nowindowborder"                        "0"               // Disable window border
    "setting.fullscreen"                            "1"               // Enable fullscreen mode
    "setting.defaultres"                            "3840"            // Default resolution width
    "setting.defaultresheight"                      "2160"            // Default resolution height
    "setting.volumetric_lighting"                   "0"               // Disable volumetric lighting
    "setting.volumetric_fog"                        "0"               // Disable volumetric fog
    "setting.mat_vsync_mode"                        "0"               // V-Sync mode (0 typically means off)
    "setting.mat_backbuffer_count"                  "0"               // Backbuffer count
    "setting.mat_antialias_mode"                    "0"               // Antialiasing mode (0 typically means off)
    "setting.csm_enabled"                           "0"               // Disable CSM (Cascaded Shadow Maps)
    "setting.csm_coverage"                          "0"               // CSM coverage
    "setting.csm_cascade_res"                       "16"              // CSM cascade resolution
    "setting.fadeDistScale"                         "0.750000"        // Fade distance scale factor
    "setting.dvs_supersample_enable"                "0"               // Disable DVS supersampling
    "setting.new_shadow_settings"                   "1"               // Enable new shadow settings
    "setting.gamma"                                 "1.000000"        // Gamma correction
    "mat_disable_bloom"                             "1"               // Disable bloom effect
    "mat_specular"                                  "0"               // Disable specular lighting
    "mat_bumpmap"                                   "0"               // Disable bump mapping
    "r_dynamic"                                     "0"               // Disable dynamic lighting
    "setting.configversion"                         "8"               // Configuration version
    "setting.dx_version_check_timestamp"            "0"               // DirectX version check timestamp
    "setting.set_dress_level"                       "1"               // Set dress level
}
 
